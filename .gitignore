# upstream scrcpy prebuilt -- not ours to redistribute, and it is 17MB
scrcpy-linux-x86_64-*/
scrcpy-linux-x86_64-*.tar.gz

# anything device- or user-specific must never be committed
s25-serial
s25-pin
*.mp4
*.png

# accessibility dumps contain whatever was on screen at the time
*ui*.xml

# local tooling state, including git worktrees created inside the repo
.claude/

# fetched by the user per README; upstream artifact
SHA256SUMS.txt
